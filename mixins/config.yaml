# Minimalistic config file for features testing
root: $PWD/mixins/source
destination: $PWD/mixins/dest
name: r-backup
exclude:
  patterns:
    - exclude
    - shouldnt-be-copied.txt
  paths:
    - truc/.ignore
include:
  - one
  - truc


